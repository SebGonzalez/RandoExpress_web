export class Personne {
  constructor(public id: number,
              public name: string,
              public firstName: string,
              public mail: string,
              public password: string
  ) {
  }
}

